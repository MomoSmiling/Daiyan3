<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daiyan.momoliu.daiyan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/AppTheme">
        <activity
            android:name=".DaiyanActivity"
            android:clickable="true"
            android:label="@string/app_name"
            android:onClick="onClick">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Me.UserActivity" />
        <activity
            android:name=".PostMomentsPage.PostMomentsActivity"
            android:label="@string/title_activity_post_moments"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Search.SearchActivity" />
        <activity android:name=".Message.MessageActivity" />
        <activity android:name=".HomePage.MomentContent.MomentContentActivity" />
    </application>

</manifest>